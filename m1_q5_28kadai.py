# リストを作成
day = ['月', '月', '火', '水', '木', '金', '金']

# removeメソッドで重複している曜日を削除（最初に見つかった'月'と'金'を1つずつ削除）
day.remove('月')  # 重複している「月」を削除
day.remove('金')  # 重複している「金」を削除

# insertメソッドで指定位置に新しい要素を追加
day.insert(5, '土')  # 金曜日の後に「土」を追加
day.insert(6, '日')  # 土曜日の後に「日」を追加

# 最後にリストを出力
print(day)

